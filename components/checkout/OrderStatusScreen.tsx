'use client';
interface Props {
  orderId: string;
  estimatedMinutes: { min: number; max: number };
}
export function OrderStatusScreen(_props: Props) {
  return <div>OrderStatusScreen (em construção)</div>;
}
